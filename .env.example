# Optional — for saving audience answers + live game sync (MC ↔ phones)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# After setting env vars, run supabase/schema.sql in Supabase SQL Editor
# and enable Realtime for table stat_active_question

# Optional — override QR link (defaults to production /audience when on localhost)
# VITE_AUDIENCE_URL=https://stat-fan-game.vercel.app/audience
